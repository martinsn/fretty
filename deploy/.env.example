# Server SSH connection
SERVER=user@your-server.com

# Domain
DOMAIN=fretty.kattenberg.se

# Secret key for JWT tokens (generate with: openssl rand -hex 32)
SECRET_KEY=change-me-to-a-random-secret-key
